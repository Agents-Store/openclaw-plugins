---
name: example-greeting
description: Greet users using the example plugin tool
---
# Example Greeting

When the user asks to be greeted or says hello, use the `example_greet` tool.

Example:
- User: "Say hi to John" → call example_greet with name="John"
- User: "Greet me" → call example_greet with the user's name if known
